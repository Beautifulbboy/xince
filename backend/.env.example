# .env
# 格式: mysql+asyncmy://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL="mysql+asyncmy://root:root@localhost:3306/xince_data"

# (可选) 用于JWT
# SECRET_KEY=...
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30